header.header
        h4.竞价
        button.btn.btn-primary.header-left(ng-click="to_list()")返回
        button.btn.btn-primary.header-right(ng-click="compete_action()" ng-disabled="usable"){{start}}
.wrapper.scrollable
       ul.list-style-2
           li(ng-repeat=" bid_list in bid_lists track by $index") {{bid_list.name}}
footer
    ul.footer-nav.footer-nav-2tabs
     li.active
       a(href="#")
        报名
     li
       a(href="#")
        竞价


