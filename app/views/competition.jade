header.header
        h4.竞价
        button.btn.btn-primary.header-left(ng-click="go_to_list()")返回
        button.btn.btn-primary.header-right(ng-click="action()" ng-disabled="use"){{start_end}}
.wrapper.scrollable
       ul.list-style-2
           li(ng-repeat=" people in persons track by $index") {{people.name}}:{{people.phone}}
footer
    ul.footer-nav.footer-nav-2tabs
     li.active
       a(href="#")
        报名
     li
       a(href="#")
        竞价


